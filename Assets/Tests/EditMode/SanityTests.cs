// Каждый импорт комментируем:
using NUnit.Framework;                 // NUnit — фреймворк модульных тестов

// Класс с простейшим тестом, чтобы проверить, что пайплайн работает
public class SanityTests
{
    [Test]                             // Атрибут помечает метод как тест
    public void AlwaysTrue()           // Название теста — произвольное
    {
        Assert.IsTrue(true);           // Собственно проверка: всегда истина
    }
}
